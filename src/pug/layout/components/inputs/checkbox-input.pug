mixin checkbox-input(name, value, label, isChecked)
    +b.input-checkbox
        +e.INPUT(
            type='checkbox'
            name= name
            value= value
            checked= isChecked
            id= value
        ).__field
        +e.LABEL(for= value).__label= label
