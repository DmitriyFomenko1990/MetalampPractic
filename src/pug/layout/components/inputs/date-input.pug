mixin date-input(title)
    +b.date-input
        +e.H3.title= title
        +e.INPUT(
            type='text'
            placeholder='ДД.ММ.ГГГГ'
            name='date'
            pattern = '[0-9]{2}-[0-9]{2}-[0-9]{4}'
        ).input